/**
 * Posições de voleibol usadas no sistema.
 */
export const LISTA_POSICOES = ['levantador', 'ponteiro', 'oposto', 'central', 'libero'];

// Posições consideradas "de ataque" para o ranking de atacantes —
// levantador e líbero ficam de fora mesmo que pontuem eventualmente,
// porque não é isso que esse ranking mede.
export const POSICOES_ATAQUE = ['ponteiro', 'oposto', 'central'];
